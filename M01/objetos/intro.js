const pessoa = {
    nome: "José",
    altura: 1.75,
    peso: 105
}

pessoa["nome"] = "Gledson"; // outra forma de se referir ao atributo de um objeto

console.log(`Meu nome é ${pessoa.nome}, tenho ${pessoa.altura} metros e peso ${pessoa.peso}kg`);