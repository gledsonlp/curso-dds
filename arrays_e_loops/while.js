const nomeDeEstudantes = ['jose', 'joao', 'maria', 'joana', 'andre'];

let indice = 0;

while (indice < nomeDeEstudantes.length) { // enquanto for menor que o número de nomes na lista
    console.log(nomeDeEstudantes[indice]);
    indice++;
}